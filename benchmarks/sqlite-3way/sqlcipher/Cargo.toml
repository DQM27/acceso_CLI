[package]
name = "brisas_sqlcipher_smoke"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false

[dependencies]
rusqlite = { version = "0.40.2", default-features = false, features = ["bundled-sqlcipher-vendored-openssl"] }
