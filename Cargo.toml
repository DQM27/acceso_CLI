[package]
name = "control_acceso"
version = "0.1.0"
edition = "2024"

[features]
default = []
dev-auth = []

[dependencies]
chrono = "0.4.45"
rusqlite = { version = "0.40.2", features = ["bundled"] }
argon2 = "0.5"
rand_core = { version = "0.6", features = ["getrandom"] }
